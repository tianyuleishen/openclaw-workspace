# 通义万相AI视频生成指南

**学习日期**: 2026-02-09
**来源**: 开发.doc + 视频制作指南.doc 合并

---

## 📋 模型速查表

### 文生图 (Text-to-Image)

| 模型 | 用途 | 价格 |
|------|------|------|
| `wan2.6-t2i` | 高质量通用 | 主流选择 |
| `wan2.5-t2i-preview` | 预览版 | 较低 |
| `wanx-v1` | 动漫风格 | ¥0.02/张 |
| `wan2.2-t2i-flash` | 快速版 | 低价 |

**推荐**: `wanx-v1` + `<3d cartoon>` 风格

### 图生视频 (Image-to-Video)

| 模型 | 用途 | 价格 |
|------|------|------|
| `wan2.6-i2v-flash` | 快速版 | ¥0.75/个 |
| `wan2.1-i2v-plus` | 高质量 | ¥2.00/个 |
| `wan2.1-kf2v-plus` | 首尾帧 | ¥0.02/个 |

**推荐**: 首尾帧模式（便宜80%）

### 首尾帧 (Keyframe-to-Video)

| 模型 | 用途 | 价格 |
|------|------|------|
| `wan2.1-kf2v-plus` | 首尾帧 | ¥0.02/个 |
| `wan2.2-kf2v-flash` | 快速首尾帧 | ¥0.04/个 |

### 图像编辑 (Image Editing)

| 模型 | 用途 | 价格 |
|------|------|------|
| `wan2.5-i2i-preview` | 多图融合 | ¥0.14/张 |
| `wanx2.1-imageedit` | 局部重绘 | ¥0.14/张 |

---

## 🎯 小爪视频制作流程

### 标准流程（推荐）

```
1. 生成首帧图片
   model: wanx-v1
   prompt: "小爪在[场景]，动作描述"
   size: 720*1280 (9:16)

2. 生成末帧图片
   model: wanx-v1
   prompt: "小爪在[场景]，结束动作"
   size: 720*1280

3. 首尾帧生成视频
   model: wan2.1-kf2v-plus
   duration: 5-15秒
   size: 720*1280
```

### 成本对比

| 方案 | 成本/个 | 5个总成本 |
|------|---------|-----------|
| 图生视频 (wan2.6-i2v-flash) | ¥0.75 | ¥3.75 |
| **首尾帧** (wan2.1-kf2v-plus) | **¥0.04** | **¥0.20** |

**节省**: 95%!

---

## 💡 提示词技巧

### 图片生成提示词结构

```
[主体描述] + [场景] + [风格] + [细节]

示例:
Cute little red lobster AI mascot '小爪' in futuristic virtual office,
holographic screens floating around, neon lights,
cyberpunk aesthetic, <3d cartoon> style
```

### 视频生成提示词结构

```
[镜头动作] + [表情] + [环境]

示例:
镜头从远处缓缓推进，小爪子歪着头，
大眼睛眨巴眨巴，充满好奇心看着镜头，
背景是科技感的办公室
```

### 提示词公式

**基础公式**:
```
提示词 = 主体 + 场景 + 风格
```

**进阶公式**:
```
提示词 = 主体(描述) + 场景(描述) + 风格(定义) + 镜头语言 + 氛围词 + 细节修饰
```

---

## 🛠️ API调用示例

### 图片生成 (wanx-v1)

```python
from dashscope import ImageSynthesis

response = ImageSynthesis.call(
    model='wanx-v1',
    prompt='Cute little lobster in virtual office',
    size='720*1280',
    style='<3d cartoon>'
)
```

### 首尾帧视频生成 (wan2.1-kf2v-plus)

```python
from dashscope import VideoSynthesis

response = VideoSynthesis.call(
    model='wan2.1-kf2v-plus',
    first_frame_url='首帧URL',
    last_frame_url='末帧URL',
    prompt='小爪子工作场景',
    extra_input={
        'duration': 15,
        'size': '720*1280'
    }
)

# 等待完成
result = VideoSynthesis.wait(task_id)
```

### 图像编辑 (wan2.5-i2i-preview)

```python
from dashscope import ImageSynthesis

response = ImageSynthesis.call(
    model='wan2.5-i2i-preview',
    prompt='给小爪戴上星星发卡',
    images=['原图URL'],
    n=1
)
```

---

## 📝 图片输入方式

支持三种方式：

1. **公网URL**
   ```python
   image_url = "https://example.com/image.png"
   ```

2. **Base64编码**
   ```python
   image_url = f"data:image/png;base64,{base64_data}"
   ```

3. **本地文件路径** (仅SDK)
   ```python
   image_url = "file:///path/to/image.png"  # Linux/Mac
   image_url = "file://C:/path/to/image.png"  # Windows
   ```

---

## 🎨 图像编辑功能

### 支持的功能

| 功能 | 说明 | 示例 |
|------|------|------|
| 扩图 | 向外扩展背景 | "向外扩图，生成远景" |
| 去水印 | 移除水印 | "去除左上角所有文字" |
| 风格迁移 | 改变风格 | "转换成法国绘本风格" |
| 指令编辑 | 修改元素 | "把头发改成红色" |
| 局部重绘 | 重绘区域 | "给图1添加星星发卡" |
| 多图融合 | 融合多图 | "把图1的闹钟放到图2餐桌旁" |

### 使用示例

```python
# 多图融合
ImageSynthesis.call(
    model="wan2.5-i2i-preview",
    prompt="将图1中的元素移到图2的背景中",
    images=[url1, url2],
    n=1
)
```

---

## 💰 成本优化策略

### 最佳实践

1. **先用首尾帧测试** (¥0.02)
   - 生成2张关键帧图片
   - 生成短视频预览效果

2. **满意后再升级** (¥0.75)
   - 用高清图生视频
   - 获得更好效果

3. **批量生产** (¥0.02/个)
   - 确认效果后批量生成
   - 使用首尾帧模式

### 成本对比

| 操作 | 单价 | 5个批量 | 100个 |
|------|------|---------|--------|
| 首尾帧 | ¥0.02 | ¥0.10 | ¥2.00 |
| 图生视频 | ¥0.75 | ¥3.75 | ¥75.00 |
| 节省 | - | **97%** | **97%** |

---

## 📚 相关文档

- 完整指南: `/home/admin/.openclaw/workspace/阿里万相视频生成指南.txt`
- 提示词模板: `/home/admin/.openclaw/workspace/memory/clawlet_fixed_style.md`
- 视频生成脚本: `/home/admin/.openclaw/workspace/scripts/`

---

**最后更新**: 2026-02-09
**版本**: v1.0
