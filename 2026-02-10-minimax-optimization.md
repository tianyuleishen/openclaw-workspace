# 2026-02-10 MiniMax 模型调用优化

## 背景

根据 MiniMax Coding Plan 限制说明：

| 限制项 | 说明 |
|--------|------|
| **计费方式** | 1 prompt ≈ 15次模型调用（打包） |
| **速率限制** | 每5小时重置，计算5小时内总量 |
| **API Key 区别** | Coding Plan Key vs 普通 Key |
| **多工具共享** | 额度共享，可切换按量付费 |

---

## 优化策略

### 1. 🎯 任务分类策略

| 任务类型 | 使用 Key | 原因 |
|----------|----------|------|
| 编程任务（代码生成、调试、重构） | Coding Plan | 套餐专用，优化成本 |
| 简单对话、问候 | 普通 Key | 避免浪费套餐额度 |
| 通用任务（写作、翻译） | Coding Plan | 批量处理更高效 |

### 2. 📦 批量处理

```
优化前: 10个独立请求 → 10次 prompt
优化后: 10个请求分2批 → 2次 prompt
节省: 80% prompt 消耗
```

代码示例：
```python
from minimax_optimizer import optimize_minimax_usage

tasks = [
    {'type': 'code', 'content': 'def func1(): pass'},
    {'type': 'explain', 'content': 'Explain this code'},
    {'type': 'debug', 'content': 'Fix this bug'},
    # ... 更多任务
]

results = optimize_minimax_usage(tasks)
```

### 3. 💾 智能缓存

- **TTL**: 1小时（根据内容哈希）
- **命中率提升**: 重复请求直接返回缓存
- **节省**: 每次缓存命中 = 1次 prompt

### 4. ⏱️ 速率控制

```python
# 监控5小时使用量
usage_5h = get_5h_usage()  # 假设当前使用450次
usage_percent = (450 / 1000) * 100  # 45%

# 超过80%时报警，超过95%时自动限流
if usage_percent > 80:
    print("⚠️ 接近限额，建议降低调用频率")
```

### 5. 🔀 混合使用策略

```python
# 任务分发逻辑
if task_is_programming():
    use_coding_plan_key()
elif task_is_simple():
    use_normal_key()
else:
    # 根据当前使用率选择
    if usage_percent < 50:
        use_coding_plan_key()
    else:
        use_normal_key()
```

---

## 预期效果

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| **Prompt 消耗** | 1000次/天 | 350次/天 | **65%↓** |
| **Token 消耗** | 100万/天 | 70万/天 | **30%↓** |
| **响应延迟** | 单次调用 | 批量调用 | **相同** |
| **缓存命中率** | 0% | 30-50% | **显著** |

---

## 代码文件

| 文件 | 功能 |
|------|------|
| `minimax_optimizer.py` | 核心优化器 |
| `minimax_config.py` | 配置和策略 |

---

## 使用示例

### 1. 批量处理
```python
from minimax_optimizer import get_optimizer

optimizer = get_optimizer()
tasks = [...]  # 任务列表
results = optimizer.process_batch(tasks)
```

### 2. 检查使用率
```python
stats = optimizer.get_stats()
print(f"5小时使用率: {stats['usage_percent']:.1f}%")
```

### 3. 获取优化建议
```python
print(optimizer.generate_report())
```

---

## 最佳实践

1. ✅ **编程任务用 Coding Plan，简单任务用普通 Key**
2. ✅ **批量处理小任务，减少 prompt 消耗**
3. ✅ **使用缓存避免重复调用**
4. ✅ **监控5小时使用量，避免触发限额**
5. ✅ **高峰期降低调用频率**

---

## 注意事项

- Coding Plan API Key 和普通 Key 不可混用
- 额度多工具共享，需协调使用
- 接近限额时自动限流保护
- 建议结合实际使用情况调整限额参数

---

## 下一步

1. [ ] 集成到 OpenClaw 认知框架
2. [ ] 绑定真实的 API Keys
3. [ ] 调整限额参数（根据实际套餐）
4. [ ] 添加监控告警
5. [ ] 定期分析优化效果
